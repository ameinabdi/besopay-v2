import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const currencyImporterSelectors = importerSelectors(
  'currency.importer',
);

export default currencyImporterSelectors;
