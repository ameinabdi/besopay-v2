import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const transactionImporterSelectors = importerSelectors(
  'transaction.importer',
);

export default transactionImporterSelectors;
