import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const locationsImporterSelectors = importerSelectors(
  'locations.importer',
);

export default locationsImporterSelectors;
