import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const permissionImporterSelectors = importerSelectors(
  'permission.importer',
);

export default permissionImporterSelectors;
