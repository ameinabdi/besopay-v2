import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const paymentLinkImporterSelectors = importerSelectors(
  'paymentLink.importer',
);

export default paymentLinkImporterSelectors;
