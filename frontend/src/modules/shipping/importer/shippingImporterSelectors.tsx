import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const shippingImporterSelectors = importerSelectors(
  'shipping.importer',
);

export default shippingImporterSelectors;
